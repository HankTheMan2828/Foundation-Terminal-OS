# The boot splash is intentionally the built-in *text* Plymouth theme
# ("details") — real kernel/systemd messages, unmodified, no fictional flavor
# (spec §3, §9). There is deliberately no custom graphical theme here.
# install/03-plymouth-grub.sh sets: plymouth-set-default-theme details
