# Live installer medium: land straight in the installer as root on tty1.
# tty2+ remain normal root shells for rescue work (Ctrl-Alt-F2).
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin root --noclear %I $TERM
