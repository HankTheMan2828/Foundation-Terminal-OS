# Packages this profile adds on top of the generic core (install/packages.txt).
# Consumed by profiles/zenbook-duo-2024/install.sh. One package per line; # = comment.

iio-sensor-proxy  # rotation (dual-panel/tablet-mode sensing)
libwacom          # bottom-panel digitizer quirks
brightnessctl     # backlight helper (scoped via polkit, not NOPASSWD sudo)

# Display stack for THIS device only. The generic core runs on the kernel VT
# with no compositor/GPU; this device's dual-panel glue drives topology via
# wlr-randr, which needs a Wayland session (see hardware/display-stack).
cage              # Wayland kiosk compositor
kitty             # graphical terminal the Hub runs in under cage
wlr-randr         # eDP-1/eDP-2 topology control
seatd             # seat management for cage
# Firefox is NOT installed for Web Access: cage is a single-app Wayland
# kiosk, and launching a second GUI browser leaves a stuck surface with no
# keyboard focus (operator report on v0.1.1). Programs → WEB ACCESS uses
# w3m inside kitty. Re-add firefox only if the display stack becomes multi-
# client and FOUNDATIONHUB_WEB_GUI is supported.
