# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# Secrets — NEVER commit API keys (spec §6; loaded from root-owned files on target)
secrets.env
aichat.env
*.key

# Local scratch
*.log
.venv/

# Flashable-ISO build scratch + output (image/build-iso.sh)
image/work/
image/out/

# Local AI staging cache (USB creator; never ship in git)
tools/usb-creator/model.gguf
tools/usb-creator/ai-runtime.tar.gz
tools/usb-creator/llama-server
tools/usb-creator/*.tmp

